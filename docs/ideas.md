# Ideas to Explore

## UI / Layout
- Light/dark mode as a toggle switch in the sidebar instead of a button
- Add a top bar (DashboardNavbar) to the panel area
- User profile switcher in the top right corner and/or sidebar
- Apple TV style user selector screen (grid of user avatars, tap to select)
- Auto user logout after a configurable idle timeout

## Functionality
- Duplicate part detection on the create part / barcode scanner screen — check if a scanned barcode or IPN already exists before creating
- Allow setting initial stock quantity when creating a part via the barcode scanner method
